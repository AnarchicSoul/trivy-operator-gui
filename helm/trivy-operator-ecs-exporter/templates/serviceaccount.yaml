{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "trivy-operator-BINARIES-ECS_EXPORTER.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "trivy-operator-BINARIES-ECS_EXPORTER.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
